// const car = document.getElementById("missile");
// const stations = document.querySelectorAll(".station");
// stations.forEach((station, index) => {
//     setTimeout(() => {
//       car.style.transform = `translateX(${station.offsetLeft}px)`;
      
//     }, index * 3000);
// });